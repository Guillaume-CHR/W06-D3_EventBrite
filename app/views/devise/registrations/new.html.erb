<div class="modal-body row pt-15">
    <div class="col-md-7 p-0 b-0">
      <div class="px-3 py-3 pt-md-1 pb-md-4 mx-auto text-center">
          <h1>The Drinker</h1>
          <p class="lead">Hola chicos y chicas! Welcome to the only legit place to organize & take part to great events in your area</p>
          <p class="lead">Retrieve all our articles</p>
          <p class="lead">Exchanges with the community through comments, likes & private messages</p>
          <p class="lead">Please login to access our content</p>
          <%= image_tag("banner_big.png", class: "w-100") %>
      </div>
    </div>

  <div class="col-md-5 px-5">
    <h1>Create your account </h1>
    <div class="px-3 py-3 pt-md-1 pb-md-4 mx-auto text-center">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render 'form', f: f%>
      <%= f.submit "Sign up", class: "btn btn-lg btn-primary btn-block" %>
    <% end %>
    </div>
  </div>
</div>
